/**
 * SocialView PWA — Configuration
 * --------------------------------
 * Paste your Meta (Facebook) App ID below to enable more reliable
 * Facebook embeds. You can get a free App ID at:
 * https://developers.facebook.com/
 *
 * Leave as empty string "" to use anonymous / no-token mode.
 */
const CONFIG = {
  // Optional: Your Meta App ID (e.g. "1234567890123456")
  META_APP_ID: "",

  // App name shown in the UI
  APP_NAME: "SocialView",

  // Theme accent color (CSS color string)
  ACCENT_COLOR: "#7c3aed",

  // Recent history limit (number of links to remember locally)
  HISTORY_LIMIT: 20,
};
